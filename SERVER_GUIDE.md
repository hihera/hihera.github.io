# 网站服务启动指南

## 1. 环境准备

### 1.1 安装 Node.js
本项目使用 Node.js 运行本地服务器，因此需要先安装 Node.js 环境：

1. 访问 Node.js 官方网站：https://nodejs.org/zh-cn/
2. 下载并安装 LTS（长期支持）版本
3. 安装完成后，打开命令提示符（CMD）或 PowerShell，输入以下命令验证安装：
   ```bash
   node -v
   npm -v
   ```
   如果能显示版本号，则表示安装成功。

## 2. 启动服务步骤

### 2.1 打开命令行工具
1. 在任务栏搜索框中输入 "cmd" 或 "PowerShell"，然后打开命令提示符或 PowerShell
2. 或者按下 `Win + R` 组合键，输入 "cmd" 并按回车

### 2.2 导航到项目目录
在命令行中输入以下命令，导航到网站项目所在的目录：

```bash
cd e:\workspace\website\hera
```

### 2.3 启动本地服务器
输入以下命令启动网站服务器：

```bash
node server.js
```

### 2.4 确认服务器启动成功
如果看到以下输出，则表示服务器已经成功启动：

```
Server running at http://localhost:3001/
```

## 3. 访问网站

在浏览器地址栏中输入以下 URL 访问网站：

```
http://localhost:3001
```

## 4. 关闭服务器

如果需要关闭服务器，可以在命令行中按下 `Ctrl + C` 组合键。

## 5. 故障排除

### 5.1 无法启动服务器
- **错误提示**："'node' 不是内部或外部命令，也不是可运行的程序或批处理文件。"
  - **解决方案**：确认 Node.js 已经正确安装，并将 Node.js 添加到系统环境变量中。

- **错误提示**："Error: listen EADDRINUSE: address already in use :::3001"
  - **解决方案**：端口 3001 已被占用，可以修改 `server.js` 文件中的端口号（第 5 行的 `PORT` 变量），例如改为 3002，然后重新启动服务器。

### 5.2 无法访问网站
- **错误提示**："无法访问此网站" 或 "localhost 拒绝了我们的连接请求"
  - **解决方案**：
    1. 确认服务器已经成功启动，并且显示 "Server running at http://localhost:3001/"
    2. 检查浏览器中输入的 URL 是否正确（应该是 http://localhost:3001，注意大小写和端口号）
    3. 尝试关闭防火墙或杀毒软件，看是否是它们阻止了连接

- **错误提示**："404 Not Found"
  - **解决方案**：确认 `index.html` 文件存在于项目根目录下。

### 5.3 页面显示异常
- **解决方案**：
  1. 清除浏览器缓存（按 `Ctrl + Shift + Delete`）
  2. 尝试使用不同的浏览器
  3. 检查服务器日志，看是否有错误信息

## 6. 注意事项

- 启动服务器后，不要关闭命令行窗口，否则服务器会停止运行
- 如果修改了 `index.html` 或其他文件，可以刷新浏览器页面查看更新
- 如果需要在后台运行服务器，可以考虑使用 `pm2` 等进程管理工具

---

如果按照以上步骤操作后仍然无法解决问题，可以尝试重新安装 Node.js 或检查系统环境变量设置。